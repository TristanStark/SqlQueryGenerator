<Window x:Class="SqlQueryGenerator.App.SelectedColumnPropertiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Propriétés de la colonne sélectionnée"
        Height="260"
        Width="460"
        MinHeight="240"
        MinWidth="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding DisplayName}"
                   FontWeight="SemiBold"
                   FontSize="16"
                   Margin="0,0,0,12"
                   TextWrapping="Wrap" />

        <CheckBox Grid.Row="1"
                  Content="Null allowed"
                  IsChecked="{Binding NullAllowed, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  Margin="0,0,0,10" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <CheckBox Content="FIXED LENGTH"
                      IsChecked="{Binding UseFixedLength, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      VerticalAlignment="Center"
                      Margin="0,0,12,0" />
            <TextBlock Text="Length"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0" />
            <TextBox Width="80"
                     Text="{Binding FixedLength, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                     IsEnabled="{Binding UseFixedLength}" />
        </StackPanel>

        <TextBlock Grid.Row="3"
                   Text="If NULL is not allowed, the generator uses NVL for Oracle/Cognos and COALESCE for SQLite/Generic. If fixed length is enabled, numeric values are left-padded with zeroes and text values are right-padded with spaces."
                   TextWrapping="Wrap"
                   Foreground="#475569" />

        <Button Grid.Row="4"
                Content="Close"
                Width="90"
                HorizontalAlignment="Right"
                Click="Close_Click" />
    </Grid>
</Window>
